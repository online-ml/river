# Unreleased

## cluster 

- Renamed `sample_weight` to `w`.

## ensemble

- Renamed `sample_weight` to `w`.

## facto

- Renamed `sample_weight` to `w`.

## forest

- Renamed `sample_weight` to `w`.

## tree

- Renamed `sample_weight` to `w`.

## metrics

- Renamed `sample_weight` to `w`.


