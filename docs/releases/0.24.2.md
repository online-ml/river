# 0.24.2 - 2026-04-15

## build

- Added `MANIFEST.in` to ensure Cython (`.pyx`, `.pxd`) and Rust sources are included in the source distribution.
- Added `Cargo.lock` to the repository for reproducible Rust builds.
